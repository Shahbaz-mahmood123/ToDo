<UserControl x:Class="ToDoApp.HomeTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToDoApp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:dragablz="clr-namespace:Dragablz;assembly=Dragablz"
            TextElement.Foreground="{DynamicResource MaterialDesignBody}"
            Background="{DynamicResource MaterialDesignBackground}"
            TextElement.FontWeight="Medium"
            TextElement.FontSize="14"
            FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto">
    <Grid>
        <Grid HorizontalAlignment="Right" Height="180"  VerticalAlignment="Bottom" Width="330" Background="Black">
            <DataGrid Height="140" Width="300"></DataGrid>
        </Grid>

        <Grid Height="180" Width="330" HorizontalAlignment="Left" Background="Black" VerticalAlignment="Bottom">
            <DataGrid Height="140" Width="300"></DataGrid>
        </Grid>
        
       

        </Grid>
</UserControl>
